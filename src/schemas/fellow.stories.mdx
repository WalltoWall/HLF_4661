import { Meta } from '@storybook/addon-docs/blocks'

import * as Storybook from '../components/Storybook'

<Meta title="Document Types/Fellow" />

# Fellow

The Fellow custom type represents a fellow and information about him/her.
Fellows are listed using the **Fellows Grid** slice on **Page** documents. It
also is used to relate a Fellow to a project.

<Storybook.CustomTypeFieldsTable
  customTypeId="fellow"
  tab="Main"
  overrides={{
    portrait: {
      description: "Portrait of the fellow used on cohort listings",
    },
    photo: {
      description: "Photo of the fellow displayed with their biography",
    }
  }}
/>
