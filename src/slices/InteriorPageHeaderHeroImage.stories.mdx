import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import faker from 'faker'
import { getGatsbyImageData } from '@imgix/gatsby'

import * as Storybook from '../components/Storybook'
import { titleize } from '../lib/titleize'

import { InteriorPageHeaderHeroImage } from './InteriorPageHeaderHeroImage'

export const heading = titleize(faker.lorem.words(4))

export const backgroundImage = getGatsbyImageData({
  url: 'https://images.prismic.io/omidyarfellows/013446b9-15de-48ac-9ecd-c8c7164db4f4_Banner-LeadershipLearning.jpg',
  aspectRatio: 3 / 4,
  layout: 'fullWidth',
})

<Meta
  title="Interior Pages/Header/Hero Image"
  component={InteriorPageHeaderHeroImage}
/>

##### Interior Page

# Hero Image

Large hero image with a heading placed over the image. This block is typically
used as the main heading for a page above its body content.

#### Slice fields

<Storybook.SliceFieldsTable
  customTypeId="interior_page"
  sliceZoneId="header"
  sliceId="hero_image"
  fieldset="non-repeat"
  overrides={{
    background_image: {
      description: 'Background image behind the heading',
      isRequired: false,
    },
  }}
/>

### With background image

The background image is placed behind the heading text.

<Canvas withSource="none">
  <Story name="With background image">
    <InteriorPageHeaderHeroImage
      nextSharesBg={false}
      heading={heading}
      backgroundImageData={backgroundImage}
    />
  </Story>
</Canvas>

### Without background image

If no background image is provided, a default background color is used instead.

<Canvas withSource="none">
  <Story name="Without background image">
    <InteriorPageHeaderHeroImage nextSharesBg={false} heading={heading} />
  </Story>
</Canvas>
